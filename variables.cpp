#include <iostream>
#include <algorithm> // for std::swap, std::max, std::min, std::reverse, std::gcd
using namespace std;

int main()
{
    int a, b;
    swap(a,b);
    max(a,b);
    min(a,b);
    int arr[] = {1, 2, 3};
    gcd(a,b);
    int integer;
    return 0;
}